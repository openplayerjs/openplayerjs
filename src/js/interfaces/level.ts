/**
 * Level
 *
 * @description Media level a source can have.
 * @interface Level
 * @export
 */
export default interface Level {
    /**
     * @type object
     */
    height: number;
    id: string;
    label: string;
}
